const superHeros: string[] = [];
const herosPower: number[] = [];

superHeros.push("Spiderman");
herosPower.push(45);

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];
const MLModels: number[][] = [
  [255, 255, 255],
  [0, 0, 0],
];

allUsers.push({ name: "Saif", isActive: true });

export {};

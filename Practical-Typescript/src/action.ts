export function sayHello(name: string): void {
  console.log(`Hello ${name}`);
}

export let person = "Sajid";

type Student = {
  name: string;
  age: number;
};

const newPerson: Student = {
  name: "Sajid",
  age: 29,
};

export default newPerson;
